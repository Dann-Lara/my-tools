/**
 * @deprecated Use `import { usePermissions } from '../lib/permissions-context'` directly.
 * This file re-exports from the context for backwards compatibility.
 */
export {
  usePermissions,
  invalidatePermissionsCache,
  type PermissionsMap,
} from '../lib/permissions-context';
